# Deepseek_Harness 工作区

| 位置 | 是什么 |
|---|---|
| `v5.0/` | **唯一主线**：ORB 策略 FSM 架构（回测 + live 共用 `orb_fsm.py`）。入口见 `v5.0/README.md` |
| `archive/` | v8.4 时代全部旧代码（ORB_strategy / live / GLM_working / ctrader / propfirm），说明见 `archive/README.md` |
| `notebook.md` | 项目笔记本（AI 托管）：「当前状态」+「持久结论」为权威 |
| `.venv/` | Python 环境（所有脚本经 `../.venv/bin/python` 运行） |
