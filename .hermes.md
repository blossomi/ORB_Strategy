# ORB 量化项目 — 工作区根上下文

## 事实
- 唯一长期记录是 `notebook.md`（AI 维护）。**开工先读，收工把新结论追加到"变更日志"。**
- 策略：NQ/ES 5 分钟 ORB 日内 —— 盘前区间定边界、开盘后收盘价突破入场、ATR 止损、持有到收盘（不封顶，吃长尾）。
- 回测引擎 NautilusTrader；实盘走 IB（`interactive_brokers` adapter）。**环境用 pixi**（`pixi install` → 环境在 `.pixi/envs/default/`，2026-09-15 起为标准）；repo 根 `./.venv` 是 uv 备选路线，两条都可用。
- 代码主线在 `v5.0/`（FSM 架构，回测/live 共用 orb_fsm.py）；旧代码全在 `archive/`（相对几何保留）。数据 parquet 在 `archive/ORB_strategy/`（v5.0 自持 `v5.0/data/` 两件套）：`nq_5min_rth` / `nq_5min_eth` / `nq_15min_rth` / `nq_continuous_1m` 及 ES 同构。
- `dsh-session-*` 是早期 Windows（Quantower/Rithmic 时代）会话的导入副本，不是当前代码。

## 命令
- 回测：`pixi run backtest`（等价 `cd v5.0 && ../.venv/bin/python orb_backtest.py`，~10 秒）
- 改 orb_fsm.py 后必跑验证链：`pixi run test` → `pixi run backtest` + `pixi run parity <基线.csv> <候选.csv>` → `pixi run verify`（A/B/C 一次跑完）
- 装依赖：改 `pixi.toml` → `pixi install` → `pixi run check-deps`（强制与 `v5.0/requirements.txt` 同版）；uv 备选：`UV_CACHE_DIR=.uv_cache uv pip install -p .venv <pkg>`
- 聚合产物用脚本读，别整份读：`archive/ORB_strategy/html_output/v8_4_trades.csv`、大 HTML（5 MB+）不进库

## 硬规则（都是踩过的坑）
1. 回测起点必须 ≥2016：2010–2015 的 Databento 数据本身稀疏不可信。
2. **任何回测先检查"每笔能否买得起 1 手"**：小权益 + 高波动年份会出现整段零交易，结果被样本选择偏差污染。
3. 突破判定必须用**收盘价**，不能用 `bar.high/low` 触及判断（存在"两边同时突破"歧义）。
4. 仓位上限不要开到超过单根 K 线成交量量级；大单被部分成交时止损必须按**累计已成交数量**挂，否则裸奔。
5. 参数搜索报 walk-forward 滚动结果，不要只报全样本最优；结论必须附样本量、区间、成本假设。
6. 交易成本/滑点假设不要为了好看而调低；不要为了迎合而优化参数。
7. **审计文件零写入**：`v5.0/live_slippage.csv` 与 `v5.0/logs/`（orb-monitor 只读消费的实盘滑点源与逐笔成交源）只能有真实运行数据。任何引擎内跑真实 live 适配层的测试/回归必须先把 `SLIP_CSV`/`LOG_DIR` 重定向到临时目录并断言零写入 —— 已踩：6 次 `pixi run test` 攒 252 行假滑点、4 次 `verify` 攒 748 笔假交易。

## 风格
中文，简洁直接不铺垫；结论和数字先行，再给依据；不确定就说不确定。
